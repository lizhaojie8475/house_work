# 家务管家部署参数记录

以下内容是部署前必须从微信公众平台和云开发控制台补齐的记录。`【待填写】`
表示仓库中尚无真实值，不能直接用于生产发布。

## 小程序与云环境

- 小程序 AppID：`【待填写】`
- 云开发环境 ID：`【待填写】`

## 订阅消息模板

- 模板 ID：`【待填写】`
- 云函数环境变量：在云开发控制台的 `reminder` 云函数上设置
  `REMINDER_TEMPLATE_ID=【上面的模板 ID】`。

真实模板选定后，把微信公众平台显示的字段逐项记录在下表：

| 字段名 | 类型 | 业务含义 | 长度限制 |
|---|---|---|---|
| `【待填写，如 thing1】` | `【待填写】` | `【待填写】` | `【待填写】` |
| `【待填写，如 phrase2】` | `【待填写】` | `【待填写】` | `【待填写】` |
| `【待填写，如 date3】` | `【待填写】` | `【待填写】` | `【待填写】` |

`cloudfunctions/reminder/lib/message.js` 中的 `thing1`、`phrase2`、`date3`
目前只是占位字段名。落地真实模板时，必须按上表核对并修改
`buildTemplateData`，同时更新字段长度限制。

`buildSummary` 已计算设计稿要求的 `headline`（例如「刷马桶已逾期 2 天」），
但 `buildTemplateData` 暂未使用它，因为真实模板字段尚未确定。录入真实模板时
应把 `headline` 接入合适字段，并补充对应测试。
